@page
@model LogisticOrderTracker.Pages.Logistics.Forwarders.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Forwarder</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Forwarder.Name" class="control-label"></label>
                <input asp-for="Forwarder.Name" class="form-control" />
                <span asp-validation-for="Forwarder.Name" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Forwarder.Id" />
            <div class="form-group">
                <div class="checkbox">
                    <label>
                        <input asp-for="Forwarder.Active" /> @Html.DisplayNameFor(model => model.Forwarder.Active)
                    </label>
                </div>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}